# ClarityOKR Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-10-31

## Active Technologies

- TypeScript 5.x (`strict: true`), Node.js 20.x runtime, Angular 17 renderer, Electron 30 main process + Electron IPC + contextBridge, Angular 17 w/ RxJS 7, Zustand-like lightweight state manager (`@ngrx/component-store`), `zod` for runtime validation, Playwright for Electron E2E tests (001-clarify-okr-flow)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript 5.x (`strict: true`), Node.js 20.x runtime, Angular 17 renderer, Electron 30 main process: Follow standard conventions

## Recent Changes

- 001-clarify-okr-flow: Added TypeScript 5.x (`strict: true`), Node.js 20.x runtime, Angular 17 renderer, Electron 30 main process + Electron IPC + contextBridge, Angular 17 w/ RxJS 7, Zustand-like lightweight state manager (`@ngrx/component-store`), `zod` for runtime validation, Playwright for Electron E2E tests

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
